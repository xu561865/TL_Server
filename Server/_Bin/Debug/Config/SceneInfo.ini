[system]
scenenumber=10			;游戏世界的场景总数

[scene0]
threadindex=0			;驱动线程的索引
name=洛阳				;场景名字
active=1				;场景是否激活
file=luoyang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene1]
threadindex=1			;驱动线程的索引
name=苏州				;场景名字
active=1				;场景是否激活
file=suzhou.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene2]
threadindex=2			;驱动线程的索引
name=大理				;场景名字
active=1				;场景是否激活
file=dali.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene3]
threadindex=3			;驱动线程的索引
name=嵩山				;场景名字
active=1				;场景是否激活
file=songshan.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene4]
threadindex=4			;驱动线程的索引
name=太湖				;场景名字
active=1				;场景是否激活
file=taihu.scn		    ;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene5]
threadindex=5			;驱动线程的索引
name=镜湖				;场景名字
active=1				;场景是否激活
file=jinghu.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene6]
threadindex=6			;驱动线程的索引
name=无量山				;场景名字
active=1				;场景是否激活
file=wuliang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene7]
threadindex=7			;驱动线程的索引
name=剑阁				;场景名字
active=1				;场景是否激活
file=jiange.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene8]
threadindex=8			;驱动线程的索引
name=敦煌				;场景名字
active=1				;场景是否激活
file=dunhuang.scn		;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene9]
threadindex=9			;驱动线程的索引
name=少林寺				;场景名字
active=1				;场景是否激活
file=shaolin.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene10]
threadindex=10			;驱动线程的索引
name=丐帮总舵			;场景名字
active=1				;场景是否激活
file=gaibang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene11]
threadindex=11			;驱动线程的索引
name=光明顶				;场景名字
active=1				;场景是否激活
file=mingjiao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene12]
threadindex=12			;驱动线程的索引
name=武当山				;场景名字
active=1				;场景是否激活
file=wudang.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene13]
threadindex=13			;驱动线程的索引
name=天龙寺				;场景名字
active=1				;场景是否激活
file=tianlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene14]
threadindex=14			;驱动线程的索引
name=凌波洞				;场景名字
active=1				;场景是否激活
file=xiaoyao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene15]
threadindex=15			;驱动线程的索引
name=峨嵋山				;场景名字
active=1				;场景是否激活
file=emei.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene16]
threadindex=16			;驱动线程的索引
name=星宿海				;场景名字
active=1				;场景是否激活
file=xingxiu.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene17]
threadindex=17			;驱动线程的索引
name=天山				;场景名字
active=1				;场景是否激活
file=tianshan.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene18]
threadindex=18			;驱动线程的索引
name=雁南				;场景名字
active=1				;场景是否激活
file=yannan.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene19]
threadindex=19			;驱动线程的索引
name=雁北				;场景名字
active=1				;场景是否激活
file=yanbei.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene20]
threadindex=20			;驱动线程的索引
name=草原				;场景名字
active=1				;场景是否激活
file=caoyuan.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene21]
threadindex=21			;驱动线程的索引
name=辽西				;场景名字
active=1				;场景是否激活
file=liaoxi.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene22]
threadindex=22			;驱动线程的索引
name=长白山				;场景名字
active=1				;场景是否激活
file=changbai.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene23]
threadindex=23			;驱动线程的索引
name=黄龙府				;场景名字
active=1				;场景是否激活
file=huanglong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene24]
threadindex=24			;驱动线程的索引
name=洱海				;场景名字
active=1				;场景是否激活
file=erhai.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene25]
threadindex=25			;驱动线程的索引
name=苍山				;场景名字
active=1				;场景是否激活
file=cangshan.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene26]
threadindex=26			;驱动线程的索引
name=石林				;场景名字
active=1				;场景是否激活
file=shilin.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene27]
threadindex=27			;驱动线程的索引
name=玉溪				;场景名字
active=1				;场景是否激活
file=yuxi.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene28]
threadindex=28			;驱动线程的索引
name=南诏				;场景名字
active=1				;场景是否激活
file=nanzhao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene29]
threadindex=29			;驱动线程的索引
name=苗疆				;场景名字
active=1				;场景是否激活
file=miaojiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene30]
threadindex=30			;驱动线程的索引
name=西湖				;场景名字
active=1				;场景是否激活
file=xihu.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene31]
threadindex=31			;驱动线程的索引
name=龙泉				;场景名字
active=1				;场景是否激活
file=longquan.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene32]
threadindex=32			;驱动线程的索引
name=武夷				;场景名字
active=1				;场景是否激活
file=wuyi.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene33]
threadindex=33			;驱动线程的索引
name=梅岭				;场景名字
active=1				;场景是否激活
file=meiling.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene34]
threadindex=34			;驱动线程的索引
name=南海				;场景名字
active=1				;场景是否激活
file=nanhai.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene35]
threadindex=35			;驱动线程的索引
name=琼洲				;场景名字
active=1				;场景是否激活
file=qiongzhou.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene36]
threadindex=36			;驱动线程的索引
name=聚贤庄				;场景名字
active=1				;场景是否激活
file=juxian.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene37]
threadindex=37			;驱动线程的索引
name=燕子坞				;场景名字
active=1				;场景是否激活
file=yanzi.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene38]
threadindex=38			;驱动线程的索引
name=一品堂				;场景名字
active=1				;场景是否激活
file=yipin.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene39]
threadindex=39			;驱动线程的索引
name=预留1				;场景名字
active=1				;场景是否激活
file=yuliu1.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene40]
threadindex=40			;驱动线程的索引
name=预留2				;场景名字
active=1				;场景是否激活
file=yuliu2.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene41]
threadindex=41			;驱动线程的索引
name=预留3				;场景名字
active=1				;场景是否激活
file=yuliu3.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本


[scene42]
threadindex=42			;驱动线程的索引
name=叛军营地			;场景名字
active=1				;场景是否激活
file=_pingpan_1.scn	    ;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene43]
threadindex=43			;驱动线程的索引
name=叛军营地			;场景名字
active=1				;场景是否激活
file=_pingpan_1.scn	;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene44]
threadindex=44			;驱动线程的索引
name=叛军营地			;场景名字
active=1				;场景是否激活
file=_pingpan_1.scn	;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene45]
threadindex=45			;驱动线程的索引
name=叛军营地			;场景名字
active=1				;场景是否激活
file=_pingpan_1.scn	;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene46]
threadindex=46			;驱动线程的索引
name=叛军营地			;场景名字
active=1				;场景是否激活
file=_pingpan_1.scn	;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene47]
threadindex=47			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene48]
threadindex=48			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene49]
threadindex=49			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene50]
threadindex=50			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene51]
threadindex=51			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene52]
threadindex=52			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene53]
threadindex=53			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene54]
threadindex=54			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene55]
threadindex=55			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene56]
threadindex=56			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene57]
threadindex=57			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene58]
threadindex=58			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene59]
threadindex=59			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene60]
threadindex=60			;驱动线程的索引
name=木人巷				;场景名字
active=1				;场景是否激活
file=murenxiang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene61]
threadindex=61			;驱动线程的索引
name=珍珑棋局				;场景名字
active=1				;场景是否激活
file=zhenlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene62]
threadindex=62			;驱动线程的索引
name=珍珑棋局				;场景名字
active=1				;场景是否激活
file=zhenlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene63]
threadindex=63			;驱动线程的索引
name=珍珑棋局				;场景名字
active=1				;场景是否激活
file=zhenlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene64]
threadindex=64			;驱动线程的索引
name=珍珑棋局				;场景名字
active=1				;场景是否激活
file=zhenlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene65]
threadindex=65			;驱动线程的索引
name=珍珑棋局				;场景名字
active=1				;场景是否激活
file=zhenlong.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene66]
threadindex=66			;驱动线程的索引
name=水牢				;场景名字
active=1				;场景是否激活
file=shuilao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene67]
threadindex=67			;驱动线程的索引
name=水牢				;场景名字
active=1				;场景是否激活
file=shuilao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene68]
threadindex=68			;驱动线程的索引
name=水牢				;场景名字
active=1				;场景是否激活
file=shuilao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene69]
threadindex=69			;驱动线程的索引
name=水牢				;场景名字
active=1				;场景是否激活
file=shuilao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene70]
threadindex=70			;驱动线程的索引
name=水牢				;场景名字
active=1				;场景是否激活
file=shuilao.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene71]
threadindex=71			;驱动线程的索引
name=大理2				;场景名字
active=1				;场景是否激活
file=dali.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene72]
threadindex=72			;驱动线程的索引
name=大理3				;场景名字
active=1				;场景是否激活
file=dali.scn			;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene73]
threadindex=73			;驱动线程的索引
name=无量山2				;场景名字
active=1				;场景是否激活
file=wuliang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene74]
threadindex=74			;驱动线程的索引
name=无量山3				;场景名字
active=1				;场景是否激活
file=wuliang.scn		;场景文件名	
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene75]
threadindex=75			;驱动线程的索引
name=剑阁2				;场景名字
active=1				;场景是否激活
file=jiange.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene76]
threadindex=76			;驱动线程的索引
name=剑阁3				;场景名字
active=1				;场景是否激活
file=jiange.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene77]
threadindex=77			;驱动线程的索引
name=地府				;场景名字
active=1				;场景是否激活
file=siwang.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=0					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene78]
threadindex=78			;驱动线程的索引
name=沙漠要塞				;场景名字
active=1				;场景是否激活
file=shamo.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本


[scene79]
threadindex=79			;驱动线程的索引
name=宋辽边境				;场景名字
active=1				;场景是否激活
file=songliao.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene80]
threadindex=80			;驱动线程的索引
name=野外匪寨				;场景名字
active=1				;场景是否激活
file=yewai.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本

[scene81]
threadindex=81			;驱动线程的索引
name=竹林				;场景名字
active=1				;场景是否激活
file=zhulin.scn			;场景文件名
serverId=0				;此场景是由哪个服务器端程序调用的
type=1					;场景类型, 如果是0，表示普通游戏场景，如果是1表示副本